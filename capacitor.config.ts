import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.madan.lorry',
  appName: 'madanlorry',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
